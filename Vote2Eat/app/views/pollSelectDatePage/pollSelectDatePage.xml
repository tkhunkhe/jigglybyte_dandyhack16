
<Page>
	<StackLayout orientation="vertical" horizontalAlignment="center" verticalAlignment="center" minWidth="200">
		<Label text="Pick the Date" />
		<DatePicker cssClass="datePickerControl" day="{{ day }}" month="{{ month }}" year="{{ year }}" />
		<Label text="Time" />

	</StackLayout>

</Page>